<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <external-path name="my_images" path="." />
    <cache-path name="my_cache_images" path="." />
    <!-- ApkInstallerPlugin used to expose its downloaded APK here for the
         package installer to read. It now streams the APK into a
         PackageInstaller session instead, which needs no URI grant, so the
         updates/ entry is gone — don't re-add it for that. -->
</paths>
